name: instructor_auto
description: "Manager Instructor Auto"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.3.4
  dio: ^5.7.0
  timeago: ^3.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  change_app_package_name: ^1.4.0

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
